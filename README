mxsnippets
==========

Simple sproutcore snippet system. 
There are also some js-snippets (useful in sinasi-mode).

usage
=====

M-x sc.*
M-x js.*

Snippets
========

sc.container  ~> SC.ContainerView...
sc.label      ~> SC.LabelView...
sc.button     ~> SC.ButtonView...
sc.list       ~> SC.ListView...
sc.scroll     ~> SC.ScrollView...
sc.segmented  ~> SC.SegmentedView...

sc.toolbar.top     ~> SC.(Top)ToolbarView...
sc.toolbar.bottom  ~> SC.(Bottom)ToolbarView...

sc.record          ~> SC.Record.attr(
sc.record.string   ~> SC.Record.attr(String, ...
sc.record.boolean  ~> SC.Record.attr(Boolean, ...
sc.record.number   ~> SC.Record.attr(Number, ...

sc.function  ~> function(){}; 
